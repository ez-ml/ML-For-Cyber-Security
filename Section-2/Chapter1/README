#List the current docker images in the local image cache .
docker images --all

#Build the docker image from "1.Dockerfile" and tag it as "ml-image-1".
docker build -t ml-image-1 -f 1.Dockerfile .

#Run the docker image 1.
docker run -p 8888:8888 ml-image-1:latest

#Build the docker image from "2.Dockerfile" and tag it as "ml-image-2". This is optimized image.
docker build -t ml-image-2 -f 2.Dockerfile .

#Run the docker image 2.
docker run -p 8888:8888 ml-image-2:latest